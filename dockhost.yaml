version: 1
project: my-project

repositories:

    fastapi:

        url: https://github.com/dockhost/example-fastapi.git
        branch: main

        # Set CPU and Memory for first container startup

        cpu: 1
        cpuFraction: 20
        memory: 256MiB